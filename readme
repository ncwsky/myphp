myphp 

这是自己初学php后慢慢做的一个简单mvc框架，也许编码水平不怎么好，但对初学php的来说还是有参考意义的。
框架取名“myphp”，支持mysql,mssql,oracle三种数据库，此框架也已经经过生产开发的考验了。
至于说明文档，暂时没有，毕竟主要是用来给初学php的人学习参考及我自己自用的。

http://git.oschina.net/dz/myphp

使用参考实例：
<?php
//定义项目路径
define('APP_PATH','./app');

// require 'conf.php'; // 这里可以载入全局配置参数数组 $cfg = array();
// 加载框架入口文件
//require("./myphp/myPhp.php");

$myphp = new myphp();	//实例化一个类
$myphp->Run();	//运行类的Run的方法
?>

cli示例：
php index.php c-a-id  默认请求模式下(url模式1示例同此示例)
php index.php c=index a=test id=23  0 普通模式下


数据库SQL日志 示例：
Log::Dir('xxx'); //可指定单独的日志目录
$db_pay = M('db_pay');
$db_pay->log_on(); //启用日志记录 仅记录execute的操作 1|2
$db_pay->log_on(0); //关闭日志记录